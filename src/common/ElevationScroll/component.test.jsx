import React from "react";
import { render } from "@testing-library/react";
import ElevationScroll from "./component";

test("Trying to render <ElevationScroll/>", () => {
  render(<ElevationScroll />);
});
