import React from "react";
import { render } from "@testing-library/react";
import UserMenuPresentation from "./component";

test("Trying to render <UserMenuPresentation/>", () => {
  render(<UserMenuPresentation />);
});
