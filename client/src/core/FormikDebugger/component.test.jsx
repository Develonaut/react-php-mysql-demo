import React from "react";
import { render } from "@testing-library/react";
import FormikDebugger from "./component";

test("Trying to render <FormikDebugger/>", () => {
  render(<FormikDebugger />);
});
